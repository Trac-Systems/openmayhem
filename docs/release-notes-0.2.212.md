# 0.2.212

OpenAI-compatible requests now translate Hugging Face video content into the
backend's native video URL form. Endpoint calibration supplies signed companion
fields for partial accepted video probes and gives required tool calls the
model's signed tool-output budget with a direct invocation prompt.

Contract version remains 25 and this release requires no pricing migration.
