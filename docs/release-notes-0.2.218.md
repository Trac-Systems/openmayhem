# 0.2.218

- Stream native OpenAI-compatible tool calls that follow assistant commentary,
  preserve the commentary, and fail closed on incomplete tool envelopes.
- Create ComfyUI request journals with inherited Windows sandbox permissions so
  reference-file cleanup and subsequent workflow requests remain reliable.
